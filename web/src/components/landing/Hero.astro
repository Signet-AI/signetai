---
---

<section class="hero">
  <canvas id="ascii-dither"></canvas>
  <canvas id="latent-topology"></canvas>

  <div class="hex-stream">
    <div class="hex-content">
      <!-- JS will populate this -->
    </div>
  </div>

  <div class="container hero-container">
    <div class="hero-grid reveal">
      <div class="hero-left">
        <div class="hero-eyebrow">Local-first agent infrastructure</div>
        <h1 class="hero-title">Your Agent's Memory. Your Machine. Every Platform.</h1>
        <p class="hero-desc">
          Portable memory and identity that lives in one <span class="text-bright">~/.agents/</span> directory — not locked inside someone else's API. Encrypted secrets your agent can use but never read. Works everywhere.
        </p>

      </div>

      <div class="hero-right">
        <div class="install-tabs">
          <button class="install-tab active" data-install="terminal">Terminal</button>
          <button class="install-tab" data-install="agent">Tell Your Agent</button>
        </div>
        <div class="install-panels">
          <div class="install-panel active" id="install-terminal">
            <div class="install-box">
              <span class="install-prompt">&gt;</span>
              <code class="install-cmd">bun add -g signetai</code>
              <button class="copy-btn push-right" aria-label="Copy install command">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13"></rect><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path></svg>
              </button>
            </div>
          </div>
          <div class="install-panel" id="install-agent">
            <div class="install-box install-box--agent">
              <code class="install-cmd">Install Signet AI, the open agent memory and identity system: https://signetai.sh/skill.md</code>
              <button class="copy-btn push-right" aria-label="Copy agent prompt">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13"></rect><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path></svg>
              </button>
            </div>
            <p class="install-hint">Copy and paste this to Claude Code, OpenClaw, or any AI agent</p>
          </div>
        </div>

        <div class="hero-actions">
          <a href="/docs" class="btn btn-primary">Read Documentation</a>
          <a href="https://github.com/Signet-AI/signetai" class="btn btn-ghost">View on GitHub</a>
        </div>

        <div class="hero-meta-grid">
          <div class="hm-item">
            <span class="hm-label">Works With</span>
            <span class="hm-val text-bright">OpenClaw, Claude Code, Cursor</span>
          </div>
          <div class="hm-item">
            <span class="hm-label">License</span>
            <span class="hm-val">Apache 2.0</span>
          </div>
          <div class="hm-item">
            <span class="hm-label">Storage</span>
            <span class="hm-val">Local-first</span>
          </div>
          <div class="hm-item">
            <span class="hm-label">Spec</span>
            <span class="hm-val">v0.2.1</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
