---
title: "Operate your installation"
description: "Run Signet as a service, edit the agent, secure access, and troubleshoot it."
---

## Install as a System Service

To have Signet start automatically on boot:

```bash
cd platform/daemon
bun run install:service
```

**macOS (launchd):**
```bash
launchctl load ~/Library/LaunchAgents/ai.signet.daemon.plist
```

**Linux (systemd):**
```bash
systemctl --user enable signet.service
systemctl --user start signet.service
```

---

## Editing Your Agent

If you enabled Signet-managed identity, your agent identity lives in four self-managed files:

**`$SIGNET_WORKSPACE/AGENTS.md`** — How the agent operates, including rules,
workflow, and constraints. This is the main file that syncs to harnesses.

**`$SIGNET_WORKSPACE/SOUL.md`** — Personality, voice, values, and temperament.

**`$SIGNET_WORKSPACE/IDENTITY.md`** — Who the agent is.

**`$SIGNET_WORKSPACE/USER.md`** — Who you are to the agent, plus preferences
and relationship context.

These files are meant to be maintained over time as durable context.

**`$SIGNET_WORKSPACE/MEMORY.md`** is different. It is auto-generated by
Signet as episodic and operational memory. Do not edit it manually.

Edit the four identity files directly in your editor or via the dashboard's
config editor. Changes sync to harnesses automatically within 2 seconds.

If you want Signet only for memory, recall, sources, and secrets, run setup with
`--identity-mode off` or choose the identity-off option in the wizard. In that
mode Signet does not create, inject, or sync `AGENTS.md`, `SOUL.md`,
`IDENTITY.md`, or `USER.md`; your harness-native instruction files remain the
source of truth.

---

## Auth and Team Deployments

By default, Signet runs in local mode with no auth required on
requests. In the default local setup, the daemon also binds to
localhost only, which keeps that unauthenticated mode local by default.
For team deployments or self-hosted remote access, Signet supports
token-based auth with roles and scopes. See [Auth](/auth/) and
[Self-Hosting](/self-hosting/) for setup details.

---

## Troubleshooting

**Daemon won't start**

Check if port 3850 is in use:
```bash
lsof -i :3850
```

Remove a stale PID file if needed:
```bash
rm $SIGNET_WORKSPACE/.daemon/pid
signet daemon start
```

**Embeddings not working**

Make sure Ollama is running:
```bash
ollama serve &
ollama pull nomic-embed-text
```

Or check that `OPENAI_API_KEY` is set in your environment (or stored
as a secret and referenced in `agent.yaml`).

**Changes not syncing to Claude Code**

Make sure `~/.claude/` exists and you have the harness configured:
```bash
ls ~/.claude/CLAUDE.md
signet status
```

**Dashboard not loading**

```bash
curl http://localhost:3850/health
signet daemon logs
```

---

## Next Steps

- [Configuration Reference](/configuration/) — all agent.yaml options
- [Memory System](/memory/) — how remember/recall works
- [Pipeline](/pipeline/) — how Pipeline V2 extracts and processes memories
- [Connectors](/connectors/) — document-source connector details
- [Remote Harness Connectors](/remote-connectors/) — connect harnesses on other machines to one Signet daemon
- [Hooks](/hooks/) — lifecycle hooks for harness integration
- [Analytics](/analytics/) — session and memory analytics
- [Diagnostics](/diagnostics/) — health checks and pipeline status
- [Documents](/documents/) — ingest files and URLs into memory
- [SDK](/sdk/) — embed Signet in your own apps
- [Auth](/auth/) — token-based auth and team deployment modes
- [Harnesses](/harnesses/) — detailed integration docs
- [API Reference](/api/) — HTTP API for scripting and tooling
