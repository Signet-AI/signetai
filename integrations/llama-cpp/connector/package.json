{
  "name": "@signet/connector-llama-cpp",
  "version": "0.113.1",
  "description": "Signet connector for llama.cpp — installs runtime chat client and session hooks",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "bin": {
    "signet-llama-chat": "./dist/signet-llama-chat.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build ../runtime/src/index.ts --outfile dist/signet-llama-chat.js --target node && bun build src/index.ts --outdir dist --target node --external better-sqlite3 && bun run build:types",
    "build:types": "tsc --emitDeclarationOnly --declaration --outDir dist",
    "dev": "bun --watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@signet/connector-base": "workspace:*",
    "@signet/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "keywords": [
    "signet",
    "llama-cpp",
    "connector",
    "ai-memory",
    "agent-identity",
    "local-llm"
  ],
  "license": "Apache-2.0"
}
